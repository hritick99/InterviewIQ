version: "3.9"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./interviewiq.db:/app/interviewiq.db
      - ./chroma_db:/app/chroma_db
      - ./logs:/app/logs
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
